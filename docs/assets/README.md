Icone e banner per README e (in futuro) per l’installer.

- `logo.svg` — marchio + nome
- `hero.svg` — fascia del README

Le icone native Tauri (`src-tauri/icons`) non stanno qui: vedi `scripts/fetch-icons.ps1` e [rilascio.md](../rilascio.md).
